{
  "name": "@meriyah-utils/monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "lerna run typecheck --stream --concurrency 1",
    "test": "lerna run test --stream --concurrency 1",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.2.4",
    "lerna": "^4.0.0",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}
